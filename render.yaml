services:
  - type: web
    name: bike-rental
    runtime: java
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/bike-rental-0.0.1-SNAPSHOT.jar
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
